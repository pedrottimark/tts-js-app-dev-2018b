{
  "name": "lesson-10-asynchronous-javascript",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "json-server --delay 1000 --watch ./00-db.json --static ."
  },
  "devDependencies": {
    "json-server": "^0.14.0"
  }
}
