'use strict';

const flour = {
  quantity: 2,
  unit: 'cup',
  stuff: 'flour',
};
const powder = {
  quantity: 1,
  unit: 'tablespoon',
  stuff: 'baking powder',
};
const salt = {
  quantity: 0.5, // 1/2
  unit: 'teaspoon',
  stuff: 'salt',
};

const ingredients = [];
// TODO push the ingredients as you wish

console.log(ingredients[0]);
console.log(ingredients[1]);
console.log(ingredients[2]);
console.log(ingredients.length);
console.log()
console.log(ingredients.length - 1);
console.log(ingredients[ingredients.length - 1]);
