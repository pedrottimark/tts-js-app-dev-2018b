'use strict';

const flour = {
  quantity: 2,
  unit: 'cup',
  stuff: 'flour',
};
const powder = {
  quantity: 1,
  unit: 'tablespoon',
  stuff: 'baking powder',
};
const salt = {
  quantity: 0.5, // 1/2
  unit: 'teaspoon',
  stuff: 'salt',
};

const ingredients = [];
// TODO const ingredientsWithFlour
// TODO const ingredientsWithFlourPowderSalt

console.log(ingredients);
// console.log(ingredientsWithFlour);
// console.log(ingredientsWithFlourPowderSalt);
