/*
import React from 'react';

import Food from './Food';

const Foods = ({addFood, foods, isFetchingIds, isRenderingFormId}) => (
  <ul className="foods">
    {foods.map(food => (
      <Food
        key={food.id}
        food={food}
        addFood={addFood}
        isDisabled={isRenderingFormId === food.id || isFetchingIds.indexOf(food.id) !== -1}
      />
    ))}
  </ul>
);
*/

export default () => null;
